import React from 'react';

const Home = () => (
  <div className="flex flex-column">
    <h1>Start Investing Today</h1>

    <h4>We'll save your information so you don't have to fixll anything out again when you come back.</h4>
  </div>
);

export default Home;
